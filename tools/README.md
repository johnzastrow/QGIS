validate_runtime.py — runtime validation helper

Usage:

```bash
# Run the validator against the repository root (default)
python tools/validate_runtime.py

# Or point at a different root using QGIS_ROOT
QGIS_ROOT="C:/path/to/tree" python tools/validate_runtime.py
```

Exit codes:
- 0: success (all required files present)

- 1: failure (missing required files)

# validate_runtime.py — runtime validation helper

Usage:

```bash
# Run the validator against the repository root (default)
python tools/validate_runtime.py

# Or point at a different root using QGIS_ROOT
QGIS_ROOT="C:/path/to/tree" python tools/validate_runtime.py
```

Exit codes:

- 0: success (all required files present)
- 1: failure (missing required files)
